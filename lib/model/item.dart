class Item {
  String name;
  int iid;
  String placeName;
  String created;
  String modified;
  bool isCardSelected;

  Item(this.name, this.iid, this.placeName, this.created, this.modified, this.isCardSelected);

}

